# 数据集预处理依赖库
# 用于将 DatasetNinja 格式转换为 YOLO 格式

# YAML 解析库 - 用于生成 data.yaml 配置文件
pyyaml>=6.0

# YOLOv11训练依赖库
# YOLOv11核心框架
ultralytics>=8.3.0

# 数据处理和分析
pandas>=2.0.0
numpy>=1.24.0

# 图像处理
opencv-python>=4.8.0
Pillow>=10.0.0

# 可视化和绘图
matplotlib>=3.7.0
seaborn>=0.12.0

# Excel导出
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# TensorBoard可视化（ultralytics已包含，显式声明）
tensorboard>=2.13.0

# 进度条
tqdm>=4.65.0
